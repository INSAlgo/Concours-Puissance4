#include <iostream>
#include <string>

using namespace std;

int main (int argc, char *argv[])
{
    string W, H, S, trash;
    cin >> W >> H >> S;

    if (S == "0")
        cin >> trash;

    while (true)
    {
        cout << 1;
        cin >> trash;
    }
    return 0;
}
