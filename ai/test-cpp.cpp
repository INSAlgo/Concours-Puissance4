#include <iostream>
#include <string>

using namespace std;

int main ()
{
    string W, H, S, trash;
    cin >> W >> H >> S;

    if (S == "0")
        cin >> trash;

    while (1)
    {
        cout << 1 << endl;
        cin >> trash;
    }

    return 0;
}
