#include <iostream>
#include <string>

using namespace std;

int main ()
{
    string W, H, N, S, trash;
    cin >> W >> H >> N >> S;

    if (S == "2")
        cin >> trash;

    while (1)
    {
        cout << 1 << endl;
        cin >> trash;
    }

    return 0;
}
